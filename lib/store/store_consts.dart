enum StoreBoolId {
  testBoolId,
  isAppFirtsOpened,
  isOnboardingFinished,
}

enum StoreIntId {
  expiresInSeconds,
}

enum StoreDoubleId {
  testDoubleId,
}

enum StoreStringId {
  longToken,
  clientId,
  appSecret,
  userID,
  dateTokenProvided,
}

enum StoreStringListId {
  testStringListId,
}

enum StoreJsonId {
  testJsonId,
}

enum StoreJsonListId {
  testJsonListId,
  tasks,
}
